# The challenge form and the endpoint it posts to. Named app_* so the firewall and
# the board's templates refer to them the same way as every other board route.
app_2fa_login:
    path: /2fa
    defaults:
        _controller: "scheb_two_factor.form_controller::form"

app_2fa_check:
    path: /2fa_check
